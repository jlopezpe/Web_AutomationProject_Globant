package Pages;

import org.openqa.selenium.WebDriver;

public class Checkout_CompletePage extends BasePage{

    //no attributes or methods needed, because we have only to do one verification
    //A success message that inform us that we purchased elements
    public Checkout_CompletePage(WebDriver driver) {
        super(driver);
    }
}
